<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css" integrity="sha384-9gVQ4dYFwwWSjIDZnLEWnxCjeSWFphJiwGPXr1jddIhOegiu1FwO5qRGvFXOdJZ4" crossorigin="anonymous">
  </head>
  <body>
    <h1><%= title %></h1>
		<!-- message -->
		<% if(msgType == 'none'){

		}else {%>
			<p class="<%= msgType %>"><%= typeof msg != 'undefined' ? msg : '' %></p>
		<%}%>
		<!-- <%= typeof msg != 'undefined' ? msg : '' %> -->

		<!-- form -->
		<form action="/upload" method="POST" enctype="multipart/form-data">
		  <div class="form-group">
		    <label for="exampleFormControlFile1">Example file input</label>
		    <input type="file" class="form-control-file" id="exampleFormControlFile1" name="myimage">
		  </div>
			<button type="submit" class="btn btn-primary">Upload</button>
		</form>
		<%if(showImage == false) {

		} else {%>
			<!--delete image-->
			<form action="/delete" method="post">
				<input style="display: none;" type="text" name="file" value="<%= typeof file != 'undefined' ? file : '' %>">
				<button type="submit" name="delete" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this image?');">Delete image</button>
			</form>
			<!-- view image -->
			<img src="<%= typeof file != 'undefined' ? file : '' %>" alt="<%= typeof file != 'undefined' ? file : '' %>">
		<%}%>
		<div>

		</div>
  </body>
</html>
